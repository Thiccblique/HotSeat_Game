You need to add 10 features. Read the file first, then implement ALL of the following in one pass. Do not skip any feature.

FEATURES TO ADD:

1. **Player colors** - Assign each player a persistent color from palette ['#7c5cfc','#0ef0c0','#ff4d8d','#ffd700','#a78bfa','#ffaa00'] when added. Show a colored dot on their name tag. Use their color for their wheel segments on the name wheel (pass per-item color array to SpinWheel). SpinWheel.draw() should use `this.colors[i]` directly when colors.length === n (items count).

2. **Round counter** - Fixed top-left display "Round X / Y". Track currentRound and maxRounds. Show during gameplay.

3. **Setup options row** - Add a full-width card at the bottom of setup-grid with:
   - Rounds picker: buttons for 5, 10, 15 (default 10 active)
   - Timer picker: Off, 30s (default), 60s, 90s
   - Wildcard toggle checkbox (default checked)
   - No-duplicates toggle checkbox (default unchecked)

4. **Wildcard slot** - If wildcard enabled, add 'WILDCARD' twice to the challenge wheel items. When WILDCARD is spun, pick a random challenge from all SUGGESTIONS categories and display it instead.

5. **Pass token** - Each player gets 1 skip per game. Add a "Skip Challenge (1 use)" button on the challenge screen (styled in pink, below/next to spin button). When clicked: re-spin challenge wheel (player's pass is consumed). Track used passes in a Set. Disable button if already used.

6. **Challenge timer** - After result is shown, show a new #timer screen with:
   - Player name + challenge text displayed
   - SVG circular countdown arc (r=44, circumference=276.46)
   - Big digit display in center
   - "Done!" button and "Skip Timer" button
   - Counts down from configured seconds (or skip straight to vote if timer=0)
   - Digit turns pink when <10s, plays a beep each second

7. **Voting screen (#vote)** - After timer: show "Did [NAME] complete their challenge?" with YES (+1 Point) and NOPE buttons. Award point to the player's score on YES.

8. **Leaderboard screen (#leaderboard)** - After vote: show sorted leaderboard with player color dots, names, points. Show "Round X of Y complete". Next Round button goes back to game screen. End Game button resets everything and goes to setup.

9. **Fullscreen button** - Fixed top-right corner, toggle fullscreen with document.requestFullscreen(). Use text "[ ]" and "X" as icons. Style subtly.

10. **Touch/swipe to spin** - On pointerdown + pointerup on wheel canvas, if movement speed > 200px/s, trigger the spin button click. Add this to both nameWheel and chalWheel canvases.

IMPLEMENTATION NOTES:
- The result screen should have a "Start Challenge" button that goes to timer screen (or vote screen if timer=Off). Keep "Spin Again" and "Edit Players" buttons too.
- The ESC key should work for new screens: timer→result, vote→result, leaderboard→stays (no ESC out)
- Update initWheels() to use player colors for name wheel segments
- Update spinAgain to reset chalSpunResult, nameSpunResult (keep scores/rounds)
- The leaderboard "Next Round" increments currentRound, calls showScreen('game') + initWheels()
- Update showResult() to also track hotSeatHistory for the no-duplicates feature
- Add background ambient music: 3 sine oscillators at 55/110/165 Hz, gain 0.04 each, start when game begins, keep running

STYLE GUIDELINES:
- All new CSS uses only the existing CSS variables: --bg, --bg2, --gold, --gold2, --purple, --purple2, --teal, --pink, --white, --muted, --card, --border
- No new colors
- Consistent with existing UI patterns (Bebas Neue for headers, DM Sans for body)
- New screens use same .screen class

Write all edits to the file. Read it first to understand the full current state before editing.
